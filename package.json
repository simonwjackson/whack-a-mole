{
  "name": "mole",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "parcel index.html",
    "publish:gh-pages": "npm run build:gh-pages && npm run push:gh-pages",
    "build:gh-pages": "parcel build index.html --public-url ./",
    "push:gh-pages": "push-dir --dir=dist --branch=gh-pages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^5.3.0",
    "parcel-bundler": "^1.9.7"
  },
  "dependencies": {
    "push-dir": "^0.4.1"
  }
}
